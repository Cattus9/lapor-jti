"use client"

import { useMemo, useState } from "react"
import { Archive, ArrowRight, BadgeCheck, Check, Handshake, MapPin, PackageSearch, Search, UserRound } from "lucide-react"
import { ContentShell } from "@/components/layout/content-shell"
import { DashboardLayout } from "@/components/layout/dashboard-layout"
import { PageHeader } from "@/components/layout/page-header"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { Dialog, DialogContent, DialogDescription, DialogTitle, DialogTrigger } from "@/components/ui/dialog"
import { Input } from "@/components/ui/input"
import { satpamHandoverHistory, type SatpamHandoverHistoryItem } from "@/features/lost-found/mock/satpam-history"
import type { CurrentUser } from "@/lib/auth/dummy-session"

function HistoryDetailDialog({ item }: { item: SatpamHandoverHistoryItem }) {
  return <Dialog><DialogTrigger render={<Button type="button" variant="outline" size="sm" className="shrink-0 bg-card hover:bg-muted" />}>Lihat detail</DialogTrigger><DialogContent><div className="border-b border-border/60 p-5 pr-14 md:p-6 md:pr-16"><div className="flex items-center gap-2 text-xs text-muted-foreground"><span className="flex size-8 items-center justify-center rounded-lg border border-border bg-background text-primary"><Archive className="size-4" aria-hidden="true" /></span><span>Riwayat penyerahan</span><span aria-hidden="true">·</span><span>{item.completedAt}</span><Badge className="ml-auto border-emerald-200 bg-emerald-50 text-emerald-700" variant="outline">Selesai</Badge></div><DialogTitle className="mt-4 text-xl leading-tight md:text-2xl">{item.title}</DialogTitle><DialogDescription className="mt-2">Pasangan laporan sudah dicocokkan, diserahkan, dan diarsipkan.</DialogDescription></div><div className="space-y-6 p-5 md:p-6"><section><div className="flex items-center gap-2"><span className="flex size-8 items-center justify-center rounded-lg border border-border bg-background text-primary"><PackageSearch className="size-4" aria-hidden="true" /></span><div><h3 className="text-sm font-semibold text-foreground">Pasangan tiket</h3><p className="mt-0.5 text-xs text-muted-foreground">Laporan yang telah dinyatakan cocok.</p></div></div><div className="mt-4 grid gap-3 sm:grid-cols-[minmax(0,1fr)_2rem_minmax(0,1fr)]"><TicketSummary label="Kehilangan" ticket={item.lostTicket} /><div className="hidden items-center justify-center sm:flex"><ArrowRight className="size-4 text-muted-foreground" aria-hidden="true" /></div><TicketSummary label="Temuan" ticket={item.foundTicket} /></div></section><section className="border-t border-border/60 pt-6"><h3 className="text-sm font-semibold text-foreground">Dasar pencocokan</h3><ul className="mt-3 space-y-2">{item.characteristics.map((characteristic) => <li key={characteristic} className="flex items-start gap-2 text-sm text-muted-foreground"><Check className="mt-0.5 size-3.5 shrink-0 text-primary" aria-hidden="true" />{characteristic}</li>)}</ul></section><section className="border-t border-border/60 pt-6"><div className="flex items-center gap-2"><span className="flex size-8 items-center justify-center rounded-lg border border-border bg-background text-primary"><Handshake className="size-4" aria-hidden="true" /></span><div><h3 className="text-sm font-semibold text-foreground">Penyerahan barang</h3><p className="mt-0.5 text-xs text-muted-foreground">Konfirmasi akhir dari proses penanganan.</p></div></div><dl className="mt-4 grid overflow-hidden rounded-xl border border-border/60 sm:grid-cols-2"><div className="border-b border-border/60 p-4 sm:border-b-0 sm:border-r"><dt className="flex items-center gap-1.5 text-xs text-muted-foreground"><UserRound className="size-3.5" aria-hidden="true" />Penerima</dt><dd className="mt-1.5 text-sm font-medium text-foreground">{item.recipient}</dd></div><div className="p-4"><dt className="flex items-center gap-1.5 text-xs text-muted-foreground"><MapPin className="size-3.5" aria-hidden="true" />Lokasi</dt><dd className="mt-1.5 text-sm font-medium text-foreground">{item.location}</dd></div></dl><div className="mt-3 rounded-xl border border-border/60 bg-background/60 p-4"><p className="text-xs text-muted-foreground">Catatan penyerahan</p><p className="mt-1.5 text-sm leading-relaxed text-foreground">{item.handoverNote}</p></div></section><section className="border-t border-border/60 pt-6"><h3 className="text-sm font-semibold text-foreground">Waktu penanganan</h3><div className="mt-3 space-y-2"><TimelineItem label="Barang dicocokkan" value={item.matchedAt} /><TimelineItem label="Barang diserahkan" value={item.handedOverAt} /><TimelineItem label="Tiket diselesaikan" value={item.completedAt} /></div><p className="mt-3 text-xs text-muted-foreground">Ditangani oleh {item.handler}</p></section></div></DialogContent></Dialog>
}

function TicketSummary({ label, ticket }: { label: string; ticket: string }) {
  return <div className="rounded-xl border border-border/60 bg-background/60 p-3"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 text-sm font-medium text-foreground">{ticket}</p></div>
}

function TimelineItem({ label, value }: { label: string; value: string }) {
  return <div className="flex items-start gap-3 rounded-xl border border-border/60 bg-background/60 p-3"><span className="mt-1.5 size-2 shrink-0 rounded-full bg-primary" aria-hidden="true" /><div><p className="text-sm font-medium text-foreground">{label}</p><p className="mt-1 text-xs text-muted-foreground">{value}</p></div></div>
}

function HistoryRow({ item }: { item: SatpamHandoverHistoryItem }) {
  return <article className="rounded-xl border border-border bg-background/60 p-4 transition-colors hover:border-primary/40 md:p-5"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div className="flex min-w-0 items-start gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-xl border border-border bg-card text-foreground shadow-2xs"><PackageSearch className="size-5" aria-hidden="true" /></span><div className="min-w-0"><h2 className="truncate text-sm font-semibold text-foreground md:text-base">{item.title}</h2><p className="mt-1 text-xs text-muted-foreground">Penerima: {item.recipient}</p></div></div><Badge className="border-emerald-200 bg-emerald-50 text-emerald-700" variant="outline">Selesai</Badge></div><div className="mt-4 grid gap-2 sm:grid-cols-[minmax(0,1fr)_2rem_minmax(0,1fr)]"><TicketSummary label="Kehilangan" ticket={item.lostTicket} /><div className="hidden items-center justify-center sm:flex"><ArrowRight className="size-4 text-muted-foreground" aria-hidden="true" /></div><TicketSummary label="Temuan" ticket={item.foundTicket} /></div><div className="mt-4 flex flex-col gap-3 border-t border-border/60 pt-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex flex-wrap items-center gap-x-4 gap-y-2 text-xs text-muted-foreground"><span className="flex items-center gap-1.5"><Handshake className="size-3.5" aria-hidden="true" />Diserahkan {item.handedOverAt}</span><span className="flex items-center gap-1.5"><UserRound className="size-3.5" aria-hidden="true" />{item.handler}</span></div><HistoryDetailDialog item={item} /></div></article>
}

export function SatpamHandoverHistory({ user }: { user: CurrentUser }) {
  const [query, setQuery] = useState("")
  const items = useMemo(() => satpamHandoverHistory.filter((item) => `${item.title} ${item.lostTicket} ${item.foundTicket} ${item.recipient}`.toLocaleLowerCase().includes(query.toLocaleLowerCase())), [query])

  return <DashboardLayout role="satpam"><ContentShell><PageHeader title="Riwayat" description={`Arsip penyerahan barang yang telah selesai, ${user.name}.`} /><Card className="shrink-0 gap-1 rounded-2xl border-border bg-sidebar p-1.5 text-sidebar-foreground shadow-xs"><div className="flex items-center gap-2 px-3 py-2 text-xs font-medium text-muted-foreground"><Archive className="size-4 text-primary" aria-hidden="true" />Riwayat penyerahan<span className="ml-auto rounded-full bg-background px-2 py-0.5 text-[11px] text-muted-foreground">{satpamHandoverHistory.length} selesai</span></div><div className="rounded-xl border border-border/60 bg-card text-card-foreground shadow-2xs"><CardContent className="space-y-4 p-4 md:p-5"><div className="flex flex-col gap-3 border-b border-border/60 pb-4 md:flex-row md:items-end md:justify-between"><div><h2 className="text-base font-semibold text-foreground">Penyerahan selesai</h2><p className="mt-1 text-sm text-muted-foreground">Pasangan laporan kehilangan dan temuan yang telah ditutup.</p></div><div className="relative w-full md:max-w-xs"><Search className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-muted-foreground" aria-hidden="true" /><Input value={query} onChange={(event) => setQuery(event.target.value)} className="h-9 bg-background pl-9" placeholder="Cari tiket, barang, atau penerima" aria-label="Cari riwayat penyerahan" /></div></div><p className="text-xs text-muted-foreground">{items.length} arsip ditemukan</p>{items.length ? <div className="space-y-3">{items.map((item) => <HistoryRow key={item.id} item={item} />)}</div> : <div className="flex min-h-52 flex-col items-center justify-center rounded-xl border border-dashed border-border bg-muted/30 px-4 text-center"><BadgeCheck className="size-6 text-muted-foreground" aria-hidden="true" /><p className="mt-3 text-sm font-medium text-foreground">Tidak ada riwayat yang sesuai</p><p className="mt-1 max-w-sm text-xs leading-relaxed text-muted-foreground">Ubah kata kunci untuk menemukan tiket atau penerima lain.</p></div>}</CardContent></div></Card></ContentShell></DashboardLayout>
}
